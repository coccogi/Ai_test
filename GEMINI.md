# Gemini CLI Project Instructions

- **Source Control:** This project is synchronized with the following GitHub repository: `https://github.com/coccogi/Ai_test.git`
- **Synchronization Policy:** Always ensure the local state is synchronized with the remote repository before and after significant changes. Use `git pull` and `git push` as needed.
